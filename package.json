{
  "name": "cloudlane",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-tenant cloud platform with Cloud Run-style deploy experience",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"cd apps/api && npm run dev\" \"cd apps/dashboard && npm run dev\"",
    "dev:api": "cd apps/api && npm run dev",
    "dev:dashboard": "cd apps/dashboard && npm run dev",
    "build": "cd apps/api && npm run build && cd ../dashboard && npm run build",
    "test": "cd apps/api && npm run test && cd ../dashboard && npm run test",
    "lint": "eslint . --ext .ts,.tsx"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "concurrently": "^8.2.2",
    "eslint": "^8.55.0",
    "typescript": "^5.3.0"
  }
}