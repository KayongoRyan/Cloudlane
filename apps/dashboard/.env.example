# Local development — copy to .env.local
# Python control plane (recommended for console + terminal):
NEXT_PUBLIC_API_URL=http://localhost:8001
# Legacy Node API (deployments only):
# NEXT_PUBLIC_API_URL=http://localhost:3001
